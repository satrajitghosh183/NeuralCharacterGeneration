# NeuralCharGen — Master Plan (C++/CUDA)

**Goal:** a few casual, inconsistent personal photos (many low-quality) → a **hyperrealistic, rigged, relightable, full-body** avatar that drops into **Unity and Unreal**. Built **entirely in C++/CUDA**. Target: a new **SIGGRAPH** paper.

Grounded in two deep-research passes (see `research-findings.md` + memory). Supersedes the prior NeRFtrinsic+MTCM+FDNeRF+marching-cubes pipeline.

---

## 1. The contribution (three legs)

> **The technical core / paper method is `docs/method.md`** (the *discovery*: casual photos are a
> hidden multi-illumination dataset ⇒ relightable avatars are identifiable with no light stage;
> recovered by a robust structured-consistency manifold inverse-rendering estimator; rendered with
> a commuting animate+relight Gaussian transport). The "three legs" below are the *system*
> framing; `method.md` is what makes it a SIGGRAPH paper rather than an integration. The pipeline
> built so far (incl. `fuse_vertex_colors`) is the **baseline row** the method ablates against.

1. **Quality/uncertainty-aware fusion of inconsistent casual photos → one canonical avatar.** Beats closest prior art **PuzzleAvatar** (SIGGRAPH Asia 2024: album→mesh, but NOT rigged, NOT relightable, SDS/not-hyperreal, no bad-image handling).
2. **A real-time C++/CUDA animatable + relightable Gaussian-splat avatar runtime, embedded in Unreal & Unity.** Research confirms **no engine plugin does animate+relight splats** — this closes a real deployment gap and is the systems contribution that justifies the all-C++/CUDA build.
3. **Learned photo/view selection for avatar reconstruction** — confirmed underexplored (only robotics NBV exists). The principled successor to the old MTCM; the guided web-capture tool supplies the casual-vs-guided ablation statistics.

## 2. Representation decision — HYBRID, gap-filling

- **Mesh + PBR (SMPL-X-rigged):** guaranteed-shippable substrate; relights natively in any engine; collisions/shadows/LODs for free. The universal fallback.
- **Custom C++/CUDA splat-avatar runtime (hero path):** our own renderer doing LBS deformation + relighting in real time, embedded natively — NOT dependent on static-only plugins. This is leg #2 of the contribution.

## 3. Per-stage stack (port pretrained weights; train only novel modules)

| Stage | Method (port to C++/CUDA) | License (research OK) |
|---|---|---|
| Bad-upload selection | NR-IQA (TOPIQ/CLIP-IQA), CR-FIQA/DSL-FIQA (face), ArcFace ID clustering, **learned selector (novel)** | pyiqa/QualiCLIP NC; reimpl for shipping |
| Body anchor | **NLF** SMPL-X regressor | code MIT / weights NC |
| Geometry (casual/sparse) | **NoPo-Avatar** (animatable Gaussian, no pose) | check |
| Geometry (clothed mesh) | MultiGO++ / FRESA | research |
| Fusion across photos | **NOVEL** (leg #1) | ours |
| Game topology | Hunyuan3D Studio PolyGen | check |
| Intrinsic decomp / delight | **IDArb** (MIT) + **StableDelight** | IDArb MIT; weights TBD |
| Rigging | **UniRig** (or inherit SMPL-X rig) → retarget Unity Humanoid / Unreal | NC |
| Runtime renderer | **NOVEL** C++/CUDA splat+LBS+relight (leg #2) | ours |

## 4. C++/CUDA system architecture (module map)

- `ncg-core` — tensor/autograd substrate (**LibTorch-backed**) + custom CUDA kernel registry (recommended; from-scratch autograd optional for owned modules).
- `ncg-io` — image/dataset loading; **weight loader** (safetensors → internal binary) + forward-parity test harness.
- `ncg-select` — NR-IQA + FIQA + ArcFace clustering + learned selector. CUDA-accelerated.
- `ncg-body` — SMPL-X eval + NLF inference.
- `ncg-recon` — Gaussian avatar + **cross-photo fusion**; **custom 3DGS rasterizer (fwd+bwd CUDA)**.
- `ncg-material` — intrinsic decomposition / delighting → PBR (albedo/roughness/normal).
- `ncg-rig` — skinning weights + skeleton; retarget to Unity Humanoid / Unreal mannequin.
- `ncg-mesh` — mesh extraction + PBR bake (universal mesh path).
- `ncg-runtime` — **the deployable real-time avatar renderer**: LBS skinning + splat rasterization + relighting.
- `ncg-unreal` / `ncg-unity` — engine integration (native C++ / native plugin).

**Build:** CMake + CUDA Toolkit + LibTorch + CUTLASS (custom GEMMs) + Eigen.

## 5. Phased roadmap (baseline-first — prove the spine before optimizing)

> **Progress log** (newest first)
> - `2026-06-27` **Phase B core GREEN — out-of-subspace Δv geometry solver + observability firewall (synthetic GT).** New `ncg-geom` module: `solve_geometry` (docs/method.md §M4/§M5) — block-coordinate descent (per-photo pose+expr ridge, shared β ridge, shared **Δv via matrix-free conjugate gradient** on Laplacian+magnitude-regularized normal equations, robust Welsch reweight) + the **observability field** o(v) (per-vertex orientation tensor of confident viewing directions → eigen-diversity) with a **hard gate** (Δv pinned 0 where o<o_solve). Proven on synthetic GT (`tests/geom/test_solve_geometry`, 5/5): on a dome mesh with a sharp off-subspace bump observed only on the left, the solver fits the data (residual 0.003), **recovers Δv where observed** (Δv·Δv_gt corr >0.5), **pins Δv=0 where unobserved** (gate), and o cleanly separates (0.99 vs 0.23). Key lesson surfaced: 2D weak-perspective shape recovery has a bas-relief/affine gauge ambiguity — only signal genuinely un-absorbable by β+affine-camera (sharp local detail, real facial geometry) lands in Δv; smooth global deformation is gauge-confounded (real-data Δv will also benefit from NLF metric vertex depth — a noted enhancement). **Remaining for Phase B:** real-data wiring (FaceMesh→SMPL-X dense embedding + PhotoBundle→solve_geometry) and the custom CUDA SpMV kernel (perf optimization; the torch-CG baseline is correct + proven). Decisions locked stand (SDS native port, hard exact-zero protection).
> - `2026-06-27` **Phase A GREEN on real data — subject gate cleanly isolates the person.** Resolved the alignment/embedder/threshold chain (debugged with a visual audit + ArcFace cosine matrix): BlazeFace detect + **6-keypoint Umeyama alignment** → **ArcFace IR-SE50** (facexlib, far more discriminative than facenet) → robust dominant-cluster gate at **keep_cos=0.5** (calibrated: same-person cosine ≳0.57, others ≲0.45). On the 67-photo album: **26 clean subject faces kept, 35 others rejected** — visual audit confirms kept=one person, dropped=other people (+ a building false-positive). Synthetic `test_subject_gate` still 8/8. Cosine matrix proved ArcFace+alignment correct (subject cluster 0.57–0.76, off-diagonal mean 0.23). Lesson: facenet/vggface2 is NOT discriminative enough on casual crops — real ArcFace + keypoint alignment is required. Assets (detector/facemesh/arcface .torchscript + ArcFace IR-SE50 weight + face_venv) on the volume. **Phase A done → advancing to Phase B (Δv solver).**
> - `2026-06-27` **Phase A real-data run — pipeline GREEN, gate quality PENDING face alignment (honest).** Built + ran the full Phase A front-end on the real 67-photo album: ported BlazeFace detector + FaceMesh-468 + facenet embedder to TorchScript (`tools/export_face_models.py`, assets on the volume), `face_models.cpp` loaders, `gate_album` orchestrator, `ncg_cli gate` (CPU, training-GPU-safe) with `--dump` visual audit. End-to-end runs: 67 photos → 33 kept / 28 rejected. **BUT the visual audit (dump of kept vs dropped crops) revealed the gate does NOT cleanly isolate the subject** — kept set is a grab-bag, not one person. Root cause: NO FACE ALIGNMENT before the identity embedder (facenet/ArcFace require a 5-point similarity transform to canonical 112²; raw bbox crops give noisy embeddings → clustering can't separate identities). The clustering MATH is proven (synthetic `test_subject_gate` P=R=1); the real-data failure is preprocessing. **Fix queued:** detector returns its 6 keypoints → C++ aligns faces → recalibrate thresholds → re-audit. Phase A NOT advanced to B until the audit shows clean subject isolation. (Disk note: `data/` can't be a bare symlink — it holds git-tracked files, so `git reset --hard` clobbers it; symlink individual untracked assets instead.)
> - `2026-06-27` **Phase A core GREEN — robust subject gate (C2 contamination rejection).** The full personalized-geometry+completion build (A→H) kicked off. Phase A interface contract frozen: `select::subject_gate` (pure dominant-identity clustering) + `body::{face_models, album_gate}` (FaceDetector/FaceMeshNet/ArcFace ports + `gate_album`/`PhotoBundle`). **`subject_gate` green on H100** (`tests/select/test_subject_gate`, 8/8): on a synthetic album (40 subject faces + 20 incidental other-person faces from 4 people) it recovers the subject as the dominant mode and rejects ALL contamination — **precision = recall = 1.0**, subject trust high, contamination Welsch trust exactly 0. Robust mode-seeking on the unit hypersphere (densest-seed → trimmed-mean centroid → keep-cos), Welsch trust on deviation-from-median distance (one-sided). This is the album-level C2 firewall proven on synthetic GT before any ArcFace weights. **Remaining for Phase A real-data run:** port the three front-end nets (MediaPipe FaceMesh 468, a multi-face detector, ArcFace) to TorchScript loaders with golden parity (assets needed on H100), then `gate_album` orchestrates them over the real 67-photo album. Decisions locked: SDS-as-loss completion, NATIVE C++/CUDA diffusion port, HARD exact-zero identity protection.
> - `2026-06-27` **Photoreal face — specular fix + hair shell + engine asset.** Three upgrades to `ncg_cli face`: (1) **specular/highlight down-weighting** in the per-texel solve — observations that are bright AND desaturated (view-dependent reflection, not albedo) are soft-gated out, removing the baked specular blotches → noticeably more even, natural skin; (2) **hair-volume shell** — segment hair from skin by the recovered albedo (dark, upper head) and offset those vertices outward along the normal (skin↔hair boundary forms a hairline). v1: adds head volume but only marginally reads as hair from the front — real strand hair is a bigger module; (3) **rigged + textured glTF export** (`write_glb_textured`) — `<prefix>_face.glb`: validated 55-joint rig + baseColor + tangent-space normal textures + full vertex attrs (POSITION/NORMAL/TEXCOORD/JOINTS/WEIGHTS), imports directly into Unity/Unreal/Blender. Finer geometry is carried by the photometric **normal map** (meso-detail the 51-landmark base mesh can't represent; >100 id dims would overfit). Remaining: strand-level hair, fuller per-texel cleanup, geometry-from-pixels refinement.
> - `2026-06-26` **Photoreal face leg — relightable skin on the recovered identity.** `ncg_cli face` now textures the personalized identity: per-photo per-vertex obs/normals/visibility (weighted by the identity estimator's trust w_i — coherent robustness) → `solve_inverse_render` (C1/C2) → relightable per-vertex albedo; then `recover_uv_albedo` lifts it to a **1024² per-texel UV albedo + photometric tangent-space normal map** (engine-ready). On the user's album: **57/67 photos usable** (the earlier "39" was a truncated log), recognizable skin-toned relightable 3D face. Per-vertex render is clean-but-smooth; per-texel adds real skin detail but carries **residual specular/seam artifacts** from harsh in-the-wild lighting (the honest gap). Remaining for "no one can tell": robust per-texel delighting (kill specular residue), hair (bald scalp is the at-a-glance tell), finer face geometry, + a textured-glTF export (`write_glb_textured`, machinery exists).
> - `2026-06-26` **Novel face-identity estimator — proven on synthetic GT, then run on the real album.** `ncg::recon::solve_face_identity` (docs/method.md Theorem 2, face restriction): robust block-coordinate factorization that fuses a casual album into ONE shared NEUTRAL identity face while factoring out each photo's expression + weak-perspective pose and rejecting wrong-person/bad-detection frames (Welsch consistency, C2). This is the multi-photo fusion NLF (single-image) cannot do — NLF gives an inconsistent identity per photo; we recover the single identity that explains the whole album. **Synthetic proof green** (`tests/recon/test_face_identity.cpp`, 6/6): neutral shape recovered to 0.0375 vs 0.914 mean-face (~24×); the CORRECT identifiability shown — VIEW/pose diversity (SfM), not expression, is what makes 3D shape recoverable from 2D landmarks; robust to 25% junk. **Real run** (`ncg_cli face --frames <album> --weights nlf --smplx ...`): NLF per-photo → 51 SMPL-X face landmarks (barycentric on `lmk_faces_idx`/`lmk_bary_coords`) → fused identity; on the user's 67-photo album → 39 usable detections, 3 auto-rejected as outliers, personalized neutral-identity mesh visibly narrower than the generic mean (geometry only — skin albedo/texture is the next module). Exporter `convert_smplx.py` now carries `face_id_dirs`/`face_expr_dirs` (100+100 of SMPL-X's 300+100 shapedirs) + the 51-landmark embedding; body `shapedirs` kept at 10 (body path unchanged).
> - `2026-06-24` **Real multi-photo delighting+relighting + rigged glTF + benchmark — the method on real data.** `ncg_cli delight` runs the inverse-rendering solver on several real casual photos (per-photo NLF pose → posed normals → sampled colors+visibility → robust multi-illumination solve) → recovered canonical albedo → relit under novel lights (validated on 3 Rock photos; consistent regions delight cleanly, inconsistent-outfit regions show the expected robustness cost). `ncg_cli benchmark` on real SMPL-X geometry gives the paper curves (C1 monotone, C2 robust ~flat vs naive 7× collapse). `mesh::write_glb_skinned` + `ncg_cli export` → valid **rigged 55-joint glTF** for Unity/Unreal (animatable). Full test suite green (44 pass / 2 design-skips). Remaining: C3 real-time animate+relight splat runtime + native engine plugins (Phase 2, the big systems lift), learned selector, C1 formal proof, user study, paper writing.
> - `2026-06-24` **Relightable avatar + engine-ready glTF export.** SMPL-X faces vendored (`convert_smplx.py` exports `f`; `SmplxModel::faces()`), enabling real vertex normals → clean Lambertian shading (no splat spray). `ncg_cli relight` renders the body under an orbiting directional light (SH shading) — relighting visible on the real body. `mesh::write_glb` + `ncg_cli export` write a valid binary glTF (POSITION/NORMAL/COLOR_0 + PBR material) — **imports directly into Unity/Unreal** (validated structurally; 10475v/20908f, ~615 KB). The "usable engine character" deliverable exists (static vertex-colored mesh; embedded skinning is the next add).
> - `2026-06-22` **Per-subject 3DGS refinement working** (`ncg_cli fit --refine`): `runtime::solve_pinhole_camera` least-squares-fits the source-photo camera (fx,fy,cx,cy; R=I,t=0) from NLF `vertices3d`↔`vertices2d`; `fit::refine_gaussians_to_image` optimizes the photo-colored body's color/opacity/scale/position against the (downscaled, `--refine_res`) photo via `render_soft`+Adam, loss masked to the body silhouette (init alpha) so background doesn't pull it. Validated on the full-body Rock photo: camera solve gives correct full-length framing + pose, refinement yields a coherent textured figure. Core novel pipeline now end-to-end: photo → NLF SMPL-X → per-vertex color → visibility → (fusion across photos) → 3DGS refinement. Remaining quality: higher-res/longer refine for sharpness, guided multi-view for back/sides, relight + runtime.
> - `2026-06-22` **Full-body textured avatar from one photo (celebrity test)** — on a clean full-body photo (Dwayne Johnson, Wikimedia), the pipeline produces a clean standing avatar with the white-shirt+jeans+face textured: **96% of vertices visible & colored** (vs 61% for upper-body photos), no spray with `--restpose`. `--detection N` picks the person in multi-person photos. Confirms: full-body input ⇒ near-complete front texture. Remaining quality gaps: splat softness (one color sample/vertex ⇒ per-subject 3DGS refinement next) and the back/sides (need rear/side photos or symmetry inpaint). `tools/dl_celeb.py`-style Commons fetch works (browser UA) for public test subjects.
> - `2026-06-22` **Real-photo textured avatar GREEN on H100** — full pipeline validated on a real photo of the user: photo → NLF SMPL-X → per-vertex photo color → visibility cull → adaptive scale → clean A-pose avatar (`ncg_cli fit --restpose 1`), recognizable face + front-torso texture. Confirmed NLF `vertices2d` IS the full 10475 mesh (appearance lights up directly). `fuse` is robust (skips no-detection views). **Finding (paper-relevant):** the user's casual library is mostly group shots / tight headshots / single shots in *different outfits*, so a clean multi-view fusion needs guided capture (front/side/back, same outfit) — casual-photo inconsistency (clothing, pose, occlusion) is exactly the problem to solve. Next: per-subject 3DGS refinement; symmetry/inpaint for unseen verts; guided-capture for real fusion coverage.
> - `2026-06-22` **Appearance capture + cross-photo fusion authored** (⏳ pending H100 build — box was unreachable): the leap from gray body → looks-like-the-person. `recon::sample_vertex_colors` (bilinear-sample the photo at NLF's `vertices2d`, color keyed by vertex identity so it's pose-independent), `recon::vertex_visibility` (point z-buffer over projected verts, rejects occluded/back-facing), `recon::fuse_vertex_colors` (confidence-weighted per-vertex color across N photos + coverage + neutral fill = **the cross-photo fusion novelty**). NLF `detect()` now returns `vertices2d`+`vertices3d`. `ncg_cli fit` paints a single photo; `ncg_cli fuse --images a,b,c` fuses several. CPU unit tests for sampling/visibility/fusion. Open: confirm NLF `vertices2d` is the full 10475 mesh (else project ourselves); add foreshortening/uncertainty weights; per-subject 3DGS refinement; back-side inpainting/symmetry.
> - `2026-06-22` **NLF END-TO-END GREEN on H100** — first real `image → NLF (ported weights) → SMPL-X pose → render`, pure C++/CUDA, no Python at runtime. Resolved the integration chain: NLF is TorchScript (`torch::jit::load`), needs `model_name="smplx"` kwarg for a 55-joint pose matching SmplxModel (default "smpl"=24j), and its detector graph calls `torchvision::nms` — which we **self-register** as our own C++ op (CPU NMS sweep, `TORCH_LIBRARY(torchvision)`) instead of dlopen'ing torchvision's Python-entangled `_C.so` (that crashes a non-Python binary). Canonicalize NLF's camera-frame (Y-down) root orientation → upright avatar. Real SMPL-X model vendored + converted (`tools/convert_smplx.py`); assets live on the H100 volume, symlinked into the repo. Validated on a synthetic body render (round-trip); **needs a real photo for a meaningful pose/appearance result.**
> - `2026-06-21` **NLF wired (port, not retrain)** — `ncg::body::Nlf` is now a `torch::jit::load` TorchScript loader, not a throwing stub: load the released `nlf_l_multi.torchscript`, run `detect_smpl_batched` on a uint8 RGB batch, pick a detection, map `pose/betas/trans` → `SmplxParams` (CPU, ready for `SmplxModel::forward`). `ncg_cli fit --image me.jpg --weights nlf_l_multi.torchscript --smplx smplx.safetensors --out posed.png` is the end-to-end image→posed-body→render path. `tools/dump_nlf.py` captures the real model's output-dict layout + a golden so the C++ parsing is confirmed (then `tests/golden/test_golden_nlf.cpp`). Decision locked: rewrite everything in C++/CUDA, **port** the commodity NLF weights (exact parity via the same TorchScript graph), train from scratch only the novel modules. ⏳ needs the downloaded NLF checkpoint + real SMPL-X to run.
> - `2026-06-21` **NeRF leg + hybrid path** (`ncg-nerf`) — ✅ GREEN on H100 (4/4 nerf tests pass): `TinyNerf` (positional-encoded MLP → density+RGB), differentiable `render_volume` (alpha-composited volume rendering matching the splat `RenderOutput`), `composite_over` (premultiplied-alpha front-to-back composite → Gaussian surface over NeRF volume = the hybrid renderer), `fit_nerf_to_views` (per-scene Adam overfit, no dataset — implicit-field counterpart to 3DGS fitting). Tests: ray unit-length/count, render shape/finiteness, composite opaque/transparent/half-cover, fit beats gray baseline. `ncg_cli nerf` demo (fit image → render; `--smplx` adds the hybrid composite dump). Answers the "keep NeRF / all-three" direction: 3DGS + real NeRF + hybrid now coexist. Single-view `fit_nerf_to_views` hardened against the dead-ReLU collapse (positive density-bias cold start + num_freqs 6→4 + lr 5e-3→1e-3 with 20-step warm-up): on a 256² body view the fit now descends `0.20 → 0.006` (~22 dB) instead of freezing at the background MSE.
> - `2026-06-19` Relighting + rigging baselines: `ncg-mesh::compute_vertex_normals`, `ncg-material::relight` (analytical Lambertian+ambient shader — real; learned IDArb decomposition stays gated), `ncg-rig::make_rigged` + `export_rigged` (OBJ + JSON skeleton/skinning; UniRig autorig + FBX/glTF gated). Tests for each. ⏳ awaiting first H100 build.
> - `2026-06-19` More phase coverage: **Phase 2 animation** (`SmplxOutput::vertex_transforms` + `recon::deform_gaussians` LBS deform, tested), **Phase 3 multi-view fit** (`fit_gaussians_to_views`, tested), **Phase 5 eval** (`runtime::orbit_trajectory` + `ncg_cli turntable`, tested), **Phase 4 geometry** (`ncg-mesh` real marching cubes + Gaussian density sampling + OBJ/PLY export, tested). Still gated: NLF weights/arch, learned relighting (IDArb), engine plugins, paper. ⏳ awaiting first H100 build.
> - `2026-06-19` Phase 2 start + SMPL-X unblock: `ncg-runtime::render_soft` (fully differentiable soft splatter, autograd reference for the CUDA rasterizer), `ncg-fit` (Adam optimization of a Gaussian cloud to a target image, per-iter loss/PSNR recording) + fit test (PSNR beats gray baseline), `ncg_cli fitimg` command. `tools/convert_smplx.py` (official `.npz`→safetensors) + `tools/make_dummy_smplx.py` (runnable UV-sphere body so the viewer runs without the licensed model). ⏳ awaiting first H100 build.
> - `2026-06-19` Data recording: `ncg-record` (timestamped run dirs, per-stage `metrics.jsonl`, image dumps, RAII timers, config snapshot) + `ncg-eval` metrics (PSNR/SSIM/MAE), wired into `ncg_viewer` so every stage records. Cross-cutting infra reused by all later phases. ⏳ awaiting first H100 build.
> - `2026-06-19` Phase 0 part 2 + Phase 1 scaffold (full): `ncg-io` (image/safetensors/WeightMap/npy), parity harness (`test_golden_linear` self-test, `tools/dump_golden.py`+`export_weights.py`, `docs/parity.md`), `ncg-body` (SMPL-X LBS forward + invariant tests; **NLF = stub port target**), `ncg-recon` (Gaussians on body), `ncg-runtime` (camera + forward-splat CUDA kernel + smoke test), `ncg-select` (sharpness), `ncg-material`/`ncg-rig`/`ncg-mesh` skeletons, `apps/ncg_viewer`+`ncg_cli`, `scripts/ci.sh`. ⏳ awaiting first H100 build; building module-by-module.
> - `2026-06-19` Phase 0 part 1: build system (CMake presets, cmake helpers, FetchContent deps), `ncg-core` (tensor/cuda/log helpers + saxpy kernel + vs-LibTorch test), `CLAUDE.md`, `docs/build.md`. ⏳ awaiting first H100 build to validate the toolchain.

- **Phase 0 — Foundations.** CMake/CUDA/LibTorch build; weight-port harness + numerical parity tests; tiny self-photo test set. *(part 1 done; part 2 = ncg-io + parity harness next)*
- **Phase 1 — Vertical slice (de-risk).** One good photo → SMPL-X fit → static Gaussian/mesh → render in our own C++/CUDA viewer. Ugly is fine; the win is an unbroken spine.
- **Phase 2 — Real-time runtime (leg #2).** Custom CUDA 3DGS rasterizer (fwd+bwd) + LBS deformation + relighting in a standalone viewer, then Unreal. The systems contribution.
- **Phase 3 — Fusion + selection (legs #1 + #3).** Multi-photo quality-aware fusion + learned selector; ablate vs single-photo / naive averaging / PuzzleAvatar.
- **Phase 4 — Relighting + universal mesh path.** Intrinsic decomposition → PBR; mesh+PBR export; Unity integration.
- **Phase 5 — Eval + paper.** Web-capture casual-vs-guided study, quantitative tables, user study, writing.

## 6. Weight-porting workflow (the timeline de-risker)
Reimplement architecture in C++/CUDA → load published weights (safetensors→binary) → **validate forward-pass numerical parity** against reference on fixed inputs → only then trust it. Train from scratch *only* the novel modules (fusion, selector, runtime).

## 7. Open / to-verify
- Topic 4: SMPL-X regressor head-to-head (NLF vs SMPLer-X/Multi-HMR/OSX) + weight licenses — only needed if commercializing; NLF is the working anchor.
- Volinga Pro (Oct 2025) reportedly relights 3DGS in Unreal — verify at build time; does not change the *animation* gap.
- Compute/VRAM/speed of IDArb/StableDelight/IQA models for batch over many uploads.
